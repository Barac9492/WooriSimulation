# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트를 이해하도록 돕는 안내서다. 삭제하지 말 것.

## 프로젝트

중고등부 **목회 계획 시뮬레이터**다. 목사·교역자가 쓰는 도구로, 친구 관계와 사역(소그룹·멘토·설교·수련회)으로 출석·신앙이 천천히 움직인다는 **전제** 위에서 공동체가 어떻게 흘러가는지 지켜본다. 바탕은 행위자 기반 모형(학생 한 명이 행위자, 1주 단위 변화)이다. 현실을 맞히는 도구가 아니다 — 그래서 결과를 "예측"이 아니라 "이 전제대로라면 이렇게 흘러간다"는 **시나리오**로 부른다. 화면 맨 위 "전제" 박스가 이 가정을 명시한다.

## 대시보드 구조 (영적 전쟁 게임, 한 화면)

목사용 한 화면이 **군사 위계 세 층**으로 흐른다(밝은 카드·평이한 문장 우선, 어려운 그래프는 접음). 영적 전쟁(엡 6) 비유.

- **전략 · 연간 전쟁** (`#war-aim`/`#war-index`) : 왜 싸우나·어디로 가나. 전세 지수(=믿음 지수) 지금→1년 뒤 전망(`projData[23]`).
- **작전 · 이번 시즌** (`#op-series`/`#op-dday`/`#op-force`/`#op-front`) : 설교 시리즈 진행 X/Y주차(`sermonProg`), 다가오는 큰 작전 D-day(`nextBigOp`), 병력(교사), 전선 상황(`rtCalc` 번짐세).
- **전술 · 이번 주 전투** (`#verdict`/`#visit-list`/`#action-one`/`#step-list` + 칩) : 신호등 전세 판정, 최전선 붙들 병사(만날 아이, `spreadScore` 상위), 이번 주 명령(추천)·STEP 1·2·3, 설교 결·프로그램 칩.
- **더 보기/자세히**(접힘) : 왜 흔들리나·효과·앞날 문장, 그리고 전염 지도·믿음 지수 그래프(`updateDiag`/`updateProj`).

`updateWar`가 세 층의 문장을 매주 채우고(가벼움), 무거운 추천·효과·앞날(`runReco`)은 생성/결정 때만 돈다. 두 축(전염=COVID·전세=Stock)은 같은 행위자 엔진을 **두 렌즈**로 본 것이다.

(옛 흐름형 ①진단 ②전망 ③결정 메모는 아래 유지)

## (옛) 대시보드 구조 메모

밝은 카드·차분한 색·실제 차트. 두 축은 같은 행위자 엔진을 **두 렌즈**로 본 것이다.

- **상단바** : 제목 + 주차/년차 + **다음 주 ▶**·**자동재생**(스페이스/→ 키) + **우리 부 설정**(서랍).
- **① 진단 — 전염 지도(COVID 렌즈)** : 든든/느슨/흔들리는 아이 + 믿음 지수 카드, **번짐세 Rₜ**(최근 2주 신규 흔들림 / 그 앞 2주, 1 넘으면 번지는 중), 주별 번짐 곡선, **관계 지도**(force 레이아웃 친구망, 상태색 · 흔들림→건강 친구 붉은 선 · 먼저 붙들 아이 붉은 테 · 거점 녹색 테), 먼저 붙들 아이 목록.
- **② 전망 — 믿음 지수(Stock 렌즈)** : 믿음 지수(0~100=`avg(헌신·신앙)×100`) 전망 차트(이대로 가면 3년, 설교 파란선·수련회 초록선), **무엇이 믿음을 움직이나**(요인 분해 막대), **위험**(최대 낙폭·나쁜 해 최악).
- **③ 무엇을 할까 — 결정 + 추천** : 설교 결(6택1)·프로그램(수련회·소그룹·멘토·전도) 칩 → "이 결정으로 보기"(일정·운영 재구성 후 처음부터) → ①② 갱신. "무엇이 가장 좋을까?"는 단일 변화들을 헤드리스로 견줘 믿음 지수를 가장 끌어올리는 것을 추천.

설정(우리 부 진단·평소 사역)은 우상단 "우리 부 설정" 서랍에 모았고, 옛 진단 입력 id(`p-*`/`s-*`)를 그대로 써서 엔진(`distFromDOM`/`captureConfig`)이 무변경으로 동작한다. "예측"이라는 말은 쓰지 않는다(맨 아래 "그냥 넣어 본 값" 한 줄로 한계를 밝힌다).

## 실행 방법

빌드 단계가 없다. `index.html`을 브라우저에서 열면 바로 돌아간다. 별도 서버, 패키지 설치, 컴파일이 필요 없다.

## 파일 구성

- `index.html` : 전체 시뮬레이션. HTML + CSS + 바닐라 JavaScript가 한 파일에 들어 있다. 외부 라이브러리 의존성 없음.
- `README.md` : 비개발자용 사용 안내.
- `CLAUDE.md` : 이 파일.

## 코드 구조 (index.html 안의 `<script>`)

엔진(모형)은 옛 버전에서 검증된 그대로 재사용한다(무변경). UI(셸·렌더)만 새 대시보드로 새로 썼다. IIFE 안에 엔진 함수가 먼저, 그 아래 UI 함수가 온다.

**엔진(라이브·헤드리스 공유)**
- `spawn`/`makeOne`/`linkIn`/`link`/`pickGrade6`/`distFromDOM` : 학생 생성·친구 연결·설정 분포 읽기.
- `tick(s,ctx)` : 학생 한 명의 1주 변화(출석힘·신앙·갈래·가정·`s.cared` 돌봄 채널). 계수는 여기 한 곳, 라이브·헤드리스 공유.
- `weekEffects`/`fires`/`periodActive`/`churchEffects`/`advanceChurch` : 일정→그 주 효과, 전체 교회·부모 층.
- `simulateOnce(cfg,weeks,policy)`/`simulate(cfg,weeks,runs,policy)` : 화면 없이 돌려 주차별 지표(믿음 지수 `idx` 포함)·`seg`·`range` 반환. `applyPolicy`는 돌봄 정책(라이브 무영향).
- `contagion()`/`friendObjs`/`spreadScore`/`studTag` : 전염 진단 계산(상태·쏠림·번짐의 핵·거점·위험 표지·`contagionDeps`).
- `segStats`/`captureConfig`/`cloneCfg`/`copyEv` : 취약 집계·설정 스냅샷.

**UI(새 대시보드)**
- `generate()` : 설정대로 생성 → `layoutNet()`(관계 지도 force 레이아웃) → `runProjection()` → `render()`.
- `step()` : 라이브 1주. 주별 신규 흔들림(`weekInc`, Rₜ용) 집계, 매년 재레이아웃. 스페이스/→ 키·자동재생 루프가 공유.
- `render()` : ① 지표 카드 + `updateDiag()` + `updateProj()`(매주).
- `updateDiag()` + `rtCalc`/`drawCurve`/`drawNet` : ① 전염 지도(번짐세 Rₜ·번짐 곡선·관계 지도·먼저 붙들 아이).
- `runProjection()`/`drawIdx()`/`updateRisk()`/`updateProj()` + `projData` : ② 믿음 지수 전망 차트(설교·수련회 이벤트 표시)·위험. 전망은 생성/결정 때만 재계산(헤드리스 72주×6회).
- `attribution()`/`updateAttr()` + `lastCtx` : ② 요인 분해 막대(`tick` 항을 그대로 읽음).
- `DEC`/`renderDecide()`/`decToSchedule()`/`applyDecision()`/`recommend()` : ③ 결정 칩·일정 재구성·추천.
- 설정 서랍(`p-*`/`s-*` 슬라이더 + 추세 프리셋), 믿음 지수 = `avg(commit·faith)×100`.

라이브와 헤드리스가 같은 `tick`을 쓰므로 모형이 일관된다. 계수는 `tick` 한 곳만 고치면 양쪽 반영. 일정 시작 주차는 절대값(1~).

<!-- 옛 픽셀 3탭 UI(관찰/시나리오/설정)와 그 렌더 함수(record/draw/renderActions/renderTimeline/setMode 등)는 은퇴했다. 아래 모형 규칙은 그대로 유효하다. -->

<details><summary>옛 함수 메모(참고용, 현재 미사용)</summary>

- `spawn(D, grade, forceEng)` : 분포 객체 `D`에 따라 학생 한 명의 속성을 만든다. 라이브와 헤드리스가 공유하는 순수 함수다.
- `makeOne(grade)` : `spawn`에 좌석·애니메이션 위상을 더한 라이브용 래퍼다. 분포는 `distFromDOM()`에서 읽는다.
- `link(s, pool, mn, mx)` : 같은/인접 학년 안에서 친구를 mn~mx명 연결한다. 내부적으로 `linkIn(s, master, pool, mn, mx)`을 부른다(친구 인덱스는 `master` 배열 기준).
- `generate()` : 시작 분포대로 전체 집단을 새로 생성한다.
- `tick(s, ctx)` : 학생 한 명의 1주 변화를 계산한다. 출석·신앙 업데이트 계수가 모두 여기 모여 있다. 라이브와 헤드리스가 공유한다.
- `step()` : 라이브 1주를 진행한다. 활성 학생마다 `tick`을 부르고, 전도 신입·이탈·진급·신입생 유입을 처리한다. 자동재생 루프와 "다음 주 ▶"(`btn-step`) 버튼이 공유한다. `setPlay(on)`이 재생 상태·버튼 라벨을 관리한다.
- `activeInputs(wk)` / `updateWeekPanel(wk)` / `updateWeekFx(cur,prev)` / `updatePreview()` : 관찰 상단의 "이번 주 입력" 칩(설교 진행 주차·수련회·시험 등), "이번 주 효과(지난주 대비)" 델타(`prevMetrics`와 비교), "다음 주 예정" 미리보기를 그린다. `record()`에서 갱신.
- `fires(ev, w)` : 점이벤트·수련회가 해당 주차에 실행되는지 판정한다.
- `seriesActive(ev, w)` : 설교 시리즈가 해당 주차에 지속 중인지 판정한다(`start`부터 `weeks`주간).
- `weekEffects(sched, w)` : 한 주의 일정을 훑어 `{f(점이벤트 플래그), retreat(참가율·강도 인수), sermon(활성 초점 배열), labels}`를 만든다. `step`과 `simulateOnce`가 공유한다.
- `simulateOnce(cfg, weeks)` / `simulate(cfg, weeks, runs)` : 화면 없이 한 시나리오를 `weeks`주 돌려 주차별 지표 기록을 반환한다. `simulate`는 `runs`회(기본 8) 평균이며, `acc.seg`(취약 집계)와 `acc.range`(최종 주차 지표의 8회 변동 폭 min~max)를 함께 붙인다. 변동 폭은 결과의 불확실성을 정직하게 드러내 단일 숫자 과신을 막는다.
- `captureConfig()` : 현재 분포·운영(`s-*` 슬라이더)·일정을 시나리오 객체 `{dist, ops, schedule}`로 스냅샷한다. 일정 시작은 절대 주차(1~)이므로 보정 없이 그대로 쓴다.
- `projectRun()` : 시나리오 모드. 현재 계획과 "기본 운영만(빈 일정)"을 같은 기간 돌려 견주고, 아래에 취약 분석을 덧붙인다.
- `contagion()` / `updateContagion(c)` / `friendObjs`·`liveFriends`·`spreadScore`·`studTag` + `#conta` + `showSpread` + `contagionDeps` : 관찰의 **전염 진단**(비유 렌즈, 한 패널로 통합). 한 아이가 흔들리면 친구도 흔들린다는 전제를 SIR식(든든·느슨·흔들리는 아이)으로 **읽기만** 한다(모형이 이미 쓰는 `tick`의 `0.11*(pull-0.5)` 친구 효과를 진단할 뿐, 새 가정 없음). 함께 흔들리기 쉬운 아이 수·쏠림(군집), 번짐의 핵(먼저 붙들 아이), 곁에서 붙드는 아이(활발 거점), 누가 잘 흔들리나(속성별 위험배수)를 보여 준다. 더해 **관계 쏠림·흔들리는 연결점**(누가 누구에게 매여 있나 — `contagionDeps`에 의존자 수를 담아, 상위 연결점이 전체의 몇 %와 이어졌는지·본인이 흔들리는데 많이 매달린 연결점 top을 짚는다; `updateStudCard`의 "이 아이를 친구로 둔 N명"도 이 값). "교실에서 전염 경로 보기"(`showSpread`)는 `draw`에서 흔들리는→건강 친구 붉은 선과 핵(붉은 테)·거점(녹색 테)을 겹쳐 그린다. `record()`가 `conta`를 갱신, `renderActions()`가 번짐 경고를 액션으로 띄운다. 수치는 현실 예측이 아니라 방향이다.
- `spreadExperiment()` / `applyPolicy(arr, policy)` / `drawCurves` + `CARE_FORCE`/`CARE_FAITH` + `s.cared` : 시나리오 **확산 차단 실험**. 같은 양의 집중 돌봄(매주 k명)을 **무작위**로 줄 때와 **번짐의 핵 우선**(`spreadScore` 상위)으로 줄 때를 견준다. `applyPolicy`가 매주 흔들리는 학생(eng<0.55) 중 k명을 `s.cared`로 지목하고, `tick`이 그 학생에게만 `CARE_FORCE`(출석힘)·`CARE_FAITH`(신앙)를 `tEff`로 희석해 더한다. `policy` 인자로 `simulateOnce`/`simulate`에 흐른다(없으면 무처치, 라이브는 `s.cared`를 안 써 영향 없음). 주별 위기 곡선 3줄 + 최종 표 + "핵 우선이 무작위보다 이탈 N명 적다" 한 줄. 백신을 길목에 놓을 때 유행이 더 꺾인다는 역학 비유다.
- `survivalOnce(cfg, weeks)` / `survivalRun()` + `#sv-run` : 시나리오 **잔존(생존) 곡선**. 지금 명단을 `s.init`으로 표시해 몇 년 흘리며, 매주 초기 학생을 아직 함께(`!left`)·졸업(`left`+`!dropped`)·이탈(`dropped`)로 갈라 누적 추적한다. 졸업은 손실이 아니고 이탈만 진짜 손실 — 그 갈림과 연도별 이탈 증가(중3→고1 절벽이 도약으로 드러남)를 곡선·숫자로 보인다. 보험·계리의 생명표/코호트 리텐션을 옮긴 것. `tick`·`weekEffects`·`advanceChurch`를 `simulateOnce`와 공유한다(돌봄 정책 없음).
- `attribution()` / `updateAttrib()` + `#attrib` + `lastCtx` : 관찰 자세히의 **요인 분해**(팩터 모델·기여도 분해 비유). 이번 주 출석힘(`tick`의 force)을 기본 흐름·친구·소그룹·멘토·가정·교회 분위기·설교·행사로 쪼개 평균 기여를 막대로 보인다(빨강=끌어내림, 초록=끌어올림). `step`이 매주 `lastCtx`에 그 주 ctx를 담아 두고, `attribution`이 그걸로 tick과 같은 항을 다시 계산한다(새 가정 없이 읽기만). 막대는 상대 비교로만. `record()`가 `updateAttrib`를 부른다.
- `reversion(s)` : 흔들리는 아이가 **일시 눌림(곧 돌아옴)**인지 **진짜 하락(손써야)**인지 가른다(평균회귀 vs 추세 비유). `eng=commit+수련회여운-시험압박`이므로, `commit`이 아직 받쳐 주는데(>=0.45) `eng`가 그보다 크게 눌렸으면(`commit-eng>=0.08`) 일시(`temp`), 아니면 진짜(`real`). `studNote`(흔들리는 아이 처방)와 `updateCareList`(행 배지 "지금 붙들어야"/"곧 돌아올 듯" + 진짜 먼저 정렬 + 머리 요약)에서 쓴다. 시험 dip에 헛심 쓰지 않게 돕는다.
- `stressRun()` + `#st-run` + `projectRun`의 최대 낙폭 : 시나리오 **스트레스 테스트**(은행 CCAR·최대 낙폭 비유). 악재 프리셋(교사 절반·수련회/설교 공백·교회 침체·소그룹 반감)을 `cloneCfg`로 각각 입혀 평소 계획과 견줘 활발·이탈이 얼마나 더 나빠지나를 표로(가장 아픈 충격=먼저 대비할 곳). `projectRun`은 평균 궤적의 **최대 낙폭**(정점→최저 활발 인원 낙폭)을 한 줄 덧붙여, 끝값이 좋아도 중간 골을 견딜 채비를 짚는다.
- `frontierRun()` + `#fr-run` : 시나리오 **배합 효율**(포트폴리오 이론·효율적 프런티어 비유). 몇 가지 손길 배합(멘토 집중·소그룹 집중·교사 증원·수련회 강화·고루 강화·지금)을 같은 기간 돌려 기대 활발(`acc` 최종)과 변동 폭(`acc.range.active` max−min=위험)으로 견준다. 다른 배합에 결과·위험 모두 밀리면 "굳이"(dominated)로 표시하고, 적은 흔들림으로 가장 많은 활발을 주는 쪽을 짚는다. 정밀 최적이 아니라 상대 비교임을 명시(거짓 최적화 경계).
- `segStats(arr)` / `segHTML(seg, weeks)` : **취약 분석**. 어떤 학생이 빠지는가를 본다. 고정 속성(신앙배경·외톨이·코어)은 누적 이탈률, 학년은 현재 위기 비율로 집계하고, 이탈이 가장 몰리는 유형과 그에 맞는 목회적 한 줄을 제시한다. `simulateOnce`가 최종 상태 `seg`를 `hist.seg`로 붙이고 `simulate`가 평균한다.
- `record()`는 관찰 모드에서 `#seg-live`에 "위기군이 누구인가"(학년·외톨이·비신앙 가정·비코어 구성)를 실시간으로 보여준다.
- 관찰은 네 구역으로 정리: **이번 주**(조작·입력) → **이번 주 할 일**(액션) → **우리 부 지금**(교실+범례+지표+상태 한 줄+효과) → **자세히 보기**(생태계·신앙의 결·위기군·돌봄·추이, 기본 접힘). 키보드 스페이스/→=다음 주, P=재생. 시험·수능 주엔 교실에 붉은 기운.
- `renderActions()` / `weeksUntil(type)` : 관찰 상단 **"이번 주 할 일"** 액션 대시보드. 위기군(추세 화살표)·외톨이·수능/전환/시험 임박·다락방·계획 공백을 시급순으로. "관련 명단·구성 자세히 보기" 링크로 상세 펼침. 잘 풀릴 땐 격려도. 현재 상태(위기군 비율·소그룹 밖 외톨이·비신앙 가정 위기·부모 다락방)와 다가오는 일정(수능·시험 임박, 중3→고1 전환 임박)을 훑어 시급(높음/중간/낮음)순으로 "지금 무엇이 필요한가 → 그래서 무엇을"을 띄운다. 목사·갱신팀이 한눈에 손볼 곳을 본다.
- `adviseRun()`의 **"지금은 우선순위가 아님"** 절 : 검증한 후보 중 효과가 작거나(이미 충분/안 닿음) 명백히 역효과(gain<−8)인 것을 "하지 말 것"으로 제시한다. 추천(do)과 함께 절약(don't)을 가른다. 노이즈 과잉 라벨을 막으려 후보는 6회 평균.
- `updateStudCard()` / `studNote(s)` + `#world` 클릭 핸들러 : 관찰 모드에서 학생(점)을 클릭하면 `selStudent`로 잡아 그 학생의 학년·배경·태그·출석·신앙·신앙의 결(말씀·거룩·소속)·친구 수와 "지금 이 아이에게 필요한 것"(목양 한 줄)을 `#stud-card`에 띄운다. `draw()`는 선택 학생의 좌석에서 친구 좌석으로 연결선을 그려 관계망을 보인다. 한 영혼 단위의 돌봄을 위한 창.
- `updateCareList()` + `#care-toggle` : **이번 주 돌봄 명단**. 출석이 무너진(`eng<0.3`) 학생을 약한 순으로 12명까지, 처지와 `studNote` 한 줄 처방과 함께 나열한다. 행 클릭(`data-care`)으로 그 학생을 `selStudent`로 선택해 카드로 드릴다운. 주일 심방·연락 명단 용도. `record()`가 펼쳐져 있을 때 실시간 갱신.
- `tornadoRun()` + `#pj-tornado-btn` : **지렛대 민감도(토네이도)**. 운영 지렛대(소그룹·멘토·교사·변화속도·신입·다락방)를 각각 현재에서 ±로 흔들어 `weeks`주 뒤 활발 인원이 얼마나 출렁이는지를 정렬 막대로 그린다(빨강=덜 하면 잃는 쪽, 초록=더 하면 얻는 쪽, 위쪽일수록 민감=먼저 손볼 곳). 주식 분석의 토네이도 차트를 옮긴 것 — "무엇에 집중하고 무엇을 아낄까"에 정면으로 답한다. `cloneCfg`+`simulate`(runs 5)로 계산, 절대값 아닌 상대 비교로 읽도록 명시.
- `adviseRun()` / `candidates()` / `scoreOf()` : 처방 엔진. 계획을 예측해 약점을 진단하고, 후보 개입(회복/양육/전도 설교, 멘토링·소그룹·교사↑, 수련회 강화)을 각각 같은 기간 돌려 개선 점수로 순위를 매겨 상위 3개를 추천한다(각 후보는 `helps`로 "닿는 곳" 표시). 각 카드의 "적용"(`bump`/`addPlanSermon`/`boostRetreatLive`)은 실제 계획·슬라이더에 반영하고 `projectRun()`을 자동 재실행해 효과를 바로 보인다. `projectRun`/`adviseRun`/`compareRun`은 무거운 계산을 `setTimeout`으로 미뤄 "돌리는 중/분석 중" 피드백이 먼저 그려진다.
- `buildSummaryText()` / `showSummary()` / `printReport(txt)` + 보조(`schedLine`/`vulnTop`/`topRx`) : **교사팀 공유 요약**. 우리 부 설정·운영·연간 계획·전망(계획 vs 기본, 시작 대비 궤적 포함)·취약 1순위·추천 처방 1순위를 한 덩어리 텍스트로 만들어 복사하게 한다. `printReport`는 새 창에 깔끔한 한 장 리포트를 띄워 인쇄·PDF 저장(담임·당회 보고용).
- `compareRun()` / `renderPair()` / `drawPairChart()` : 두 시나리오(A/B 또는 계획/기본)를 추이 그래프(활발 인원)와 지표 델타 표로 그린다. `renderPair` 맨 위에 평이한 판정 한 줄("~쪽이 낫다/비슷/약하다")과 "시작 대비" 궤적을 띄우고, 아래에 8회 변동 폭을 붙여 단일 숫자 과신을 막는다. A/B는 빈 슬롯을 콕 집어 안내한다. 접근성: 본문/힌트 대비는 WCAG 4.5:1을 맞췄다(Lighthouse 접근성 100).
- `draw(t)`, `drawSpark()`, `record()` : 관찰 모드의 캔버스·추이 그래프·지표 카드를 갱신한다.
- `churchEffects(sched,w)` / `advanceChurch(cc,pv,sched,w,sp,dara)` : **전체 교회 층**. 담임 말씀·전교인 수련회·부흥회·절기가 교회 분위기(`churchClimate`)를 움직이고, 그것과 다락방 참여율이 부모 다락방 활성도(`parentVitality`)를 움직인다. 라이브는 전역 `churchClimate`/`parentVitality`, 헤드리스는 run마다 로컬 `cc`/`pv`. `tick`에 `ctx.parentVitality`/`churchClimate`/`allRetreat`/`revival`로 전달되어 `fam`을 끌고 출석·신앙을 받친다.
- `renderCSched()` / `csyncFields()` + `cbtn-add` : 설정 탭의 "전체 교회 일정"(`churchSchedule`, 별도 트랙) 추가·삭제.
- `updateEco()` : 관찰 상단 "교회 생태계"(교회 분위기·부모 다락방 활성도 막대) 갱신.
- `renderTimeline()` + `TRACKS`/`activeForTypes()` + `btn-runplan` : 설정 탭 맨 위 **주간 계획 캘린더**(간트식). 사역별 **트랙이 행**, 주차가 **열**인 그리드다. 트랙은 담임 설교·어른 행사·중고등부 설교·중고등부 사역·외부 환경 다섯. 각 (트랙,주차) 칸은 그 주 그 트랙에 일정이 있으면 이벤트 색으로 칠해져 다주 이벤트가 막대로 보인다(title에 이름). 칸을 누르면 추가 폼 시작 주차가 그 주로. "이 계획대로 돌려보기"는 `generate()`+`setMode('observe')`. `renderSched`/`renderCSched` 갱신 때 함께 다시 그린다. 가로 스크롤, 좌측 트랙 라벨 sticky.
- **자동재생은 기본 꺼짐**(`playing=false`). 계획을 먼저 짜고 "다음 주 ▶"나 "자동재생"으로 돌린다. 로드 시 멋대로 돌지 않는다.
- `setMode(m)` / `syncFields()` : 관찰(`observe`)·시나리오(`scenario`)·설정(`config`) 세 탭 토글(셋 중 하나만 표시), 일정 추가 폼의 종류별 입력 표시.
- `planSnapshot()` / `applyPlan()` / `loadPlans()` 등 : 운영·연간 계획·교회 설정을 이름 붙여 `localStorage`(`woori_plans_v1`)에 저장·불러오기·삭제. 슬라이더 원시값과 일정을 그대로 직렬화한다.

라이브(`step`/`tick`)와 헤드리스(`simulateOnce`)는 같은 `tick`·`spawn`·`weekEffects`를 쓰므로 모형이 일관된다. 계수를 바꿀 때는 `tick` 한 곳만 고치면 양쪽에 반영된다. **일정의 시작 주차는 절대값(계획 시작=0주 기준 1~)이다.**

</details>

## 모형 규칙 (확장 시 일관성 유지)

기본 보정은 **분당우리교회 중고등부 한 부(약 150명)** 기준이다(교사 ~25명, 연간 신입 ~50명). 1·2부 분리는 아직 안 했다.

**학년은 중1~고3 여섯이다**(`grade` 1~6, `GRADE_LABELS`). 진단은 **중등(중1~3)·고등(고1~3) 비중** 두 슬라이더(`p-mid`/`p-high`)로 받아 `pickGrade6`가 6학년을 뽑는다. 매년(24주) 진급하며 고3(`grade 6`)이 졸업한다. 신입은 중1.

**중3→고1 전환 절벽** : 매년 중3(`grade 3`)이 고1로 올라갈 때, 헌신이 약한 학생 일부가 이탈한다(`cliffProb` = `clamp((0.55-commit)+외톨이0.15+비신앙0.10-코어0.25, 0, 0.7)`). 한국 교회 다음세대 이탈의 가장 큰 지점을 모형에 담은 것. 건강한 부는 거의 안 빠지고, 쇠퇴 중인 부는 전환에서 크게 빠진다.

**수능은 고3(`grade 6`)에만** 깊게 걸린다(시험기간과 별개, 위 '외부 환경' 참조).

**시작은 "현재 진단"이다(갱신 도구의 핵심).** "우리 교회 현재 진단" 패널에서 규모·**현재 활발/위기 비율**(`p-active`/`p-risk`)·학년·배경·코어·외톨이를 넣으면 `spawn`이 그 건강 분포대로 시작 `commit`/`faith`를 뽑는다(코어는 활발 쪽, 외톨이·비신앙은 위기 쪽). 추세 프리셋(쇠퇴/정체/성장) 버튼이 활발·위기·다락방을 한 번에 잡는다. 즉 "지금 우리(쇠퇴 중일 수도)"에서 출발해 "이 계획이 우리를 들어올리는가"를 본다 — 이상적 기본값이 아니라.

### 출석은 2층 구조다

출석을 두 층으로 나눠 실제처럼 천천히 움직이고, 시험·수련회의 일시 변동이 영속 손상으로 굳지 않게 한다.

- `commit`(느린 헌신 0~1) : 사역이 천천히 쌓아 올리는 바탕. 친구 효과도 이 값 기준.
- `eng`(표시 출석) = `clamp(commit + 0.20*retreatGlow - examPress)`. 화면 지표·이탈 판정은 이 값으로 한다.

학생 속성: `grade`, `bg`(mota/normal/none), `core`, `loner`, `commit`, `eng`, `faith`(0~1), `word`·`holy`·`belong`(신앙의 갈래 말씀·거룩·소속 0~1), `fam`(가정 신앙 0~1), `sg`, `mentor`, `sermonAcc`(설교 누적), `retreatGlow`(수련회 여운), `examPress`(시험 일시 압박), `friends`, `left`, `dropped`(출석 바닥으로 이탈했는지 — 졸업과 구분해 취약 분석 이탈률에 쓴다).

**신앙의 갈래(다면 효과)** : `faith`(종합)와 별개로 `word`(말씀)·`holy`(거룩)·`belong`(소속) 세 갈래가 있다. 매주 각 갈래는 `faith` 쪽으로 천천히 모이되(`0.05*(faith-갈래)`), 설교 결이 갈래마다 다르게 기울인다. **`SERMON_FX`** 표가 결마다 `{eng, faith, word, holy, belong}` 효과를 정의한다 — 여기에 트레이드오프가 산다. `faith`와 생태계 계수는 건드리지 않고(안전), 갈래는 그 위에 얹힌 탐색 렌즈다. 관찰의 "신앙의 결" 줄과 "이번 주 효과"의 `결` 줄에 평균·델타가 뜬다.

매주 `commit` 변화량(헌신에 쌓이는 느린 힘):
```
force = -0.045
  + 0.11*(친구 평균 commit - 0.5)   // 친구 효과
  + (소그룹? 0.06:0)*교사효율
  + (멘토? 0.05:0)*교사효율
  + 0.03*(가정신앙 - 0.5)
  + 약간의 무작위
  + 점이벤트(소그룹집중 +0.06, 봉사 +0.04, 전도 +0.03)
  + 설교 누적(engB*sermonAcc)
commit += speed * force      // speed=변화 속도 슬라이더(0.2~1.0, 기본 0.4)
```
**변화 속도**가 낮을수록 모든 변화가 작고 느려진다(끈기·관성↑). 신앙도 `faith += speed * fd`로 함께 느려진다.

**입력은 두 종류다 — 우리가 정하는 사역(지렛대)과 닥치는 외부 환경.** 설교·수련회·전도·소그룹·봉사는 교역자가 정한다. **시험기간·수능**은 닥치는 외부 환경이다(연간 계획 폼에서 optgroup으로 갈라 둠). 이 입력들이 관찰·시나리오의 출석·신앙(출력)을 움직인다.

**시험기간(일시 변동, 영속 아님)** : 일정에 `weeks`(기본 3주)와 `repeat`를 둔 기간 이벤트. 기간 동안 `examPress`가 매주 `+0.14`(상한 0.30) 쌓여 표시 출석을 끌어내리고, 끝나면 `×0.45`로 빠르게 풀려 회복된다. 기간이 이탈 기준(6주)보다 짧아 시험 단독으로는 이탈하지 않는다.

**수능(고3, 외부 환경)** : 일정에 `weeks`(기본 6주)·`repeat`(보통 24=매년). 그 시즌 동안 **고3은 깊게**(`examPress +0.20`, 상한 0.45) 빠지고 다른 학년은 옅게(`+0.05`, 상한 0.18). 끝나면 회복되지만 고3은 곧 졸업해 떠난다. 시험기간과 같은 `examPress` 채널을 쓰되 학년별로 다르게 누른다.

**수련회(상세)** : 일정에 `nights`(1·2·3박), `attendRate`, `intensity`. `factor = (0.45 + 0.22*nights) * (0.5 + 0.6*intensity)`. 그 주에 학생은 `attendRate`(코어·소그룹원 ×1.3) 확률로 참가하고, 참가자만 `retreatGlow = max(현재, factor)`(일시 급등)와 `commit += 0.02*factor`(작은 영속 상승)를 받는다. `retreatGlow`는 매주 `×0.85`로 식는다. 비참가자는 못 받아 격차가 생긴다.

**설교 시리즈** : 일정에 `focus`와 `weeks`. 초점은 여섯 — 양육·전도·회복·**말씀·거룩·은혜**(`SERMON_FOCUS`). 즉효가 아니라 시차를 두고 `sermonAcc`(매주 `+=(1-acc)*0.15`, 안 닿으면 `×0.92`)에 비례해 누적된다. 효과는 결마다 다르다(`SERMON_FX`, 트레이드오프): **거룩·순결형**은 거룩을 크게 올리되 출석(force)·소속을 누르고, **말씀형**은 말씀을 깊게 올리되 출석을 살짝 누르며, **은혜·위로형**은 소속·출석을 올리되 거룩은 더디다. 양육·전도·회복형은 신앙(`+0.022*acc`)과 출석을 함께 올린다(기존과 동일). 시리즈가 끝나도 여운이 남는다.

**상호작용(설교×관계 뿌리)** : 설교 효과는 한 아이가 관계에 얼마나 뿌리내렸나(`root=0.85+소그룹 0.18+친구수×0.04`)에 곱해진다. 소그룹·친구가 든든한 아이에겐 더 박히고, 외톨이에겐 덜 박힌다. 그래서 외톨이는 "더 좋은 설교"가 아니라 **관계로 먼저 잇는 것**이 답 — 같은 레버라도 결과가 아이마다·부서마다 달라지는(안 뻔한) 비선형이다. `root` 평균은 ≈1로 두어 전체 보정은 유지.

### 3층 교회 생태계 (중고등부는 진공이 아니다)

고등학생의 신앙·출석은 청소년 사역만으로 정해지지 않는다. 위 두 층이 천장을 정한다.

- **① 전체 교회** : 별도 트랙 `churchSchedule`(담임 말씀 시리즈·전교인 수련회·부흥회·절기)이 `churchClimate`(0~1)를 천천히 끌어올린다. 목표값 = `0.45 + 이벤트 부스트`, 매주 그쪽으로 `sp*0.25` 이동.
- **② 가정·부모** : `parentVitality`(부모 다락방 활성도) 목표 = `0.22 + 0.46*churchClimate + 0.30*다락방참여율`, 매주 `sp*0.20` 이동.
- **③ 중고등부** : 각 학생 `fam`이 `parentVitality`(+배경 보정: 모태 +0.12, 비신앙 −0.18) 쪽으로 매주 `sp*0.10` 이동한다(이제 고정값이 아니다). `fam`은 출석힘에 `0.05*(fam−0.5)`, 신앙에 `0.020*(fam−0.5)`. 더해 `churchClimate`가 직접 출석 `0.035*(cc−0.5)`·신앙 `0.015*(cc−0.5)`를 받친다.
- **전교인 수련회**(`allRetreat`) : 온 가족 참여 → 모든 학생에게 수련회 여운(glow 0.9)+commit·신앙 상승. **부흥회**(`revival`) : 그 기간 출석 `+0.05`·신앙 `+0.02`.

이로써 전체 교회·부모가 살아나면 중고등부가 눈에 띄게 따라 오르고, 청소년 사역만으로는 닿지 못하는 천장이 보인다. 수치는 전부 가정값 — 정밀 예측이 아니라 "무엇이 천장을 정하는가"의 방향으로 읽는다.

교사효율 = min(1, (교사수 * 8) / 활동 학생 수). 학생이 많아질수록 개입 효과가 희석된다.

표시 출석이 6주 연속 바닥(0.12 미만)이면 이탈 처리된다. 24주(1년)마다 학년이 오르고 3학년은 졸업해 빠지며, 신입생이 들어온다.

## 중요한 한계 (반드시 유지할 전제)

위의 계수(0.18, 0.11 등)는 모두 **임의로 설정한 가정값**이며 실제 데이터로 검증되지 않았다. 이 수치를 사실처럼 다루지 말 것. 실제 결론을 내리는 용도가 아니라 가설을 다듬는 용도임을 코드 주석이나 화면 문구에 유지하라.

## 작성 규칙

- UI 텍스트와 주석은 한국어, 신문 칼럼체(~다/~이다). 대화체 종결어미(~이에요/~거든요) 사용 금지.
- 글에 em dash(—) 사용 금지.
- 화면에 나오는 모든 숫자는 반올림 처리(`Math.round` 등).
- 비주얼은 **깔끔한 현대 대시보드**(밝은 회색 배경·흰 카드·차분한 색·읽기 쉬운 sans-serif). 색은 CSS 변수(`--good`/`--bad`/`--covid`/`--stock` 등)로 모았다. 옛 픽셀 팔레트는 폐기.
- 전문 용어(SIR·Rₜ·포트폴리오·VaR 등)는 쉬운 말로 옮겨 쓴다(목사가 주 사용자). 모든 수치는 "그냥 넣어 본 값, 방향으로만"의 한계를 유지.

## 좋은 다음 작업 (사용자가 요청할 만한 것)

1. **1·2부 분리** : 각 ~150명을 부로 나눠 따로 돌리고 부별/합계를 본다(친구·교사·정원이 부 안에서 닫힘). 사용자가 다음 단계로 남겨 둔 것.
2. 실제 중고등부 출석 데이터(CSV)를 불러와 시작 분포와 계수를 보정하기.
3. 예측 결과·처방을 목회 보고용 한 장짜리 리포트(또는 PDF)로 내보내는 버튼.
4. 연간 계획·운영·교회 설정을 저장/불러오기(localStorage 또는 JSON).
5. 예측 결과에 신뢰구간(평균의 분산) 표시.
6. 시험기간 출석 하락폭(`examPress` 상한 0.30)과 설교 초점별 효과를 실제 경험에 맞게 미세 조정.
