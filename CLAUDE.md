# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트를 이해하도록 돕는 안내서다. 삭제하지 말 것.

## 프로젝트

중고등부 **목회 계획 시뮬레이터**다. 목사·교역자가 쓰는 도구로, 친구 관계와 사역(소그룹·멘토·설교·수련회)으로 출석·신앙이 천천히 움직인다는 **전제** 위에서 공동체가 어떻게 흘러가는지 지켜본다. 바탕은 행위자 기반 모형(학생 한 명이 행위자, 1주 단위 변화)이다. 현실을 맞히는 도구가 아니다 — 그래서 결과를 "예측"이 아니라 "이 전제대로라면 이렇게 흘러간다"는 **시나리오**로 부른다. 화면 맨 위 "전제" 박스가 이 가정을 명시한다. 이 도구는 `DIAGNOSIS.md`의 다섯 단계 중 **양육(내면화 `owned`)·잔존(졸업 신앙 `carry`)** 두 가지를 특권화한다. 닿음(미등록 풀)·번짐(리더 세움)은 모형 밖이고, 지금은 진단(읽기)만 하며 추적되는 돌봄(쓰기)은 아직 없다.

## 대시보드 구조 (현재: 두 탭 분석 대시보드)

빌드 없는 단일 `index.html`이다. Chart.js v4(CDN)로 그린다. **라이브(주별 진행) 모드는 없다.** 시나리오마다 `simulate(cfg, 52, 6)`를 화면 없이 한 번 돌려(`buildScenario`, `SC_CACHE`에 캐시) 그 결과를 정적 차트로 보여 준다. 상단바(제목·부제 `#subtitle`, 탭 토글, 시나리오 선택 `#scenario-sel`, 명단 CSV 업로드, 샘플 양식, 인쇄)와 접이식 계획 편집기, 두 탭으로 구성된다.

- **담임 요약(`#view-pastor`)** : 숫자 카드 넷(현재 활발·위기군·평균 신앙·누적 이탈, 시작 대비 증감), 활발·위기·신앙 추이 라인(`#c-trend`: 6회 변동 띠·연간 일정 음영·호버 추적기), 종합 판정 한 줄(`#verdict`), 계획 vs 기본 운영 비교(`#c-compare-pastor`).
- **교사팀 상세(`#view-team`)** : 출석 구성, 위기군 도넛, 신앙 추이, 신앙의 갈래, 설교 결별 효과, 수련회 박수별 효과, 계획 vs 기본 상세+표, 취약 분석 표, 지렛대 민감도(토네이도), 업로드 명단 표.
- **계획 편집기(접힘)** : 평소 사역 슬라이더(소그룹·멘토·교사·변화속도·다락방·신입)와 연간 일정 추가. "이 계획으로 보기"가 현재 명단을 시작 인구로 두고 내 계획으로 `SC_DEFS.custom`을 만들어 전체를 다시 그린다. 저장·불러오기(localStorage `woori_plan_v1`).

시나리오는 다섯이다: `우리 부 데이터`(CSV/샘플 seed, 기본)·`계획`·`기본 운영만`·`쇠퇴 부`(가정 분포)·`내 계획`(편집기). 화면에 "예측"이라는 말은 쓰지 않는다(가정값·방향 참고).

## 실행 방법

빌드 단계가 없다. `index.html`을 브라우저에서 열면 바로 돌아간다. 별도 서버, 패키지 설치, 컴파일이 필요 없다.

## 파일 구성

- `index.html` : 전체 시뮬레이션. HTML + CSS + 바닐라 JavaScript가 한 파일에 들어 있다. 외부 라이브러리 의존성 없음.
- `README.md` : 비개발자용 사용 안내.
- `sample-data.csv` : 백데이터 샘플 명단(112명). 머리글 `이름,학년,신앙배경,코어,외톨이,출석상태`. 업로드 양식이자 기본 표시 데이터(같은 내용이 `index.html`에 `SAMPLE_CSV`로 내장돼 file://·오프라인에서도 동작한다).
- `DIAGNOSIS.md` : "교회학교가 약해졌다"는 말의 분해. 대시보드보다 먼저 오는 정의 합의 도구 ("약해졌다"=닿음·양육·잔존·번짐·역량 다섯 주장의 분해, 우리탓/환경탓 분모 가르기, 이해관계자 불일치 설문). 도구가 어느 정의를 특권화하는지 밝힌다.
- `SURVEY.md` : 진단 설문 설계(교사 관찰 1차, 검증 척도 RCI-A·FMS-12 출처).
- `ONTOLOGY.md` : 교회 온톨로지(Palantir식) 설계도 — 객체·링크·행동·함수·권한.
- `METHODOLOGY.md` : 방법론·진단의 인용 가능한 학술 근거(ABM·전염·측정척도·생존분석·조기경보) + 엔진 매핑.
- `CLAUDE.md` : 이 파일.

## 코드 구조 (index.html 안의 `<script>`)

엔진(헤드리스)이 먼저 오고 그 아래 UI(렌더)가 온다. 전역 `var`로 top-level 선언이다(인라인 onclick에서 부르므로 IIFE가 아니다). 라이브 모드가 없어 `tick`의 단일 소비자는 `simulateOnce`다.

**엔진 (순수, 화면 없음)**
- `spawn(D, grade, forceEng)` : 분포 `D`로 학생 한 명을 만든다. `pickGrade6`·`linkIn`이 학년·친구를 잇는다.
- `tick(s, ctx)` : 학생 한 명의 1주 변화. 주간 동역학 계수가 여기 모인다(시작 분포·전환·생태계 계수는 별도, 아래 모형 규칙).
- `weekEffects`/`fires`/`periodActive`/`churchEffects`/`advanceChurch` : 일정에서 그 주 효과, 전체 교회·부모 층.
- `simulateOnce(cfg, weeks)` : 한 번 돌려 주차별 `hist`를 반환한다(`active`/`loose`/`risk`/`f`/`own`/`carry` 등과 `seg` 취약 집계). `cfg.seed`가 있으면 CSV 명단을 `cloneStudent`로 시드하고, 없으면 `spawn` 경로를 쓴다.
- `simulate(cfg, weeks, runs)` : `runs`회(기본 6) 평균에 주별 최소~최대 띠(`aLo`/`aHi`·`rLo`/`rHi`·`fLo`/`fHi`)를 붙인다.
- `fragility`/`cliffProb`/`teacherEff`/`segStats` : 취약도·전환 절벽·교사 효율·취약 집계.

**백데이터 (CSV 명단)**
- `parseCSV`/`splitCSVLine`(따옴표·BOM 처리) → `studentFromRow`(행을 시작 속성으로) → `makeDataDef`(`rosterToDist`로 분포 도출) → `SC_DEFS.data`. 업로드는 `onCSV`/`loadRoster`. 업로드 전엔 내장 `SAMPLE_CSV`(=`sample-data.csv`)가 기본이다.

**시나리오·렌더**
- `SC_DEFS`(시나리오 정의)·`buildScenario(key)`(plan·base·sermon·retreat·tornado·events를 한 번에 계산)·`sc()`(`SC_CACHE` 캐시)·`setTab`/`loadScenario`.
- `renderAll()` : 카드마다 `safe()`로 격리한다(한 카드가 throw해도 나머지는 그리고 `#render-warn` 배너로 알린다). 탭별 렌더러는 `renderKPI`·`renderTrend`·`renderVerdict`·`comparePair`·`renderAttendance`·`renderCrisis`·`renderFaith`·`renderBranches`·`renderSermon`·`renderRetreat`·`renderCompareTable`·`renderVuln`·`renderTornado`·`renderRoster`.
- 차트 공통은 `mkChart`/`baseOpts`/`weeklyOpts`. 불확실성 띠는 `bandDS`(범례·툴팁에서 숨김). 호버 추적기·연간 일정은 `crosshairPlugin`/`eventBandsPlugin`/`eventsForWeek`/`weekStory`/`calendarSummary`. 비율은 `pct(a,b)`로 분모 0을 막는다.
- 계획 편집기는 `pe-*` 슬라이더와 `peApply`/`peAddEvent`/`peReadOps`/`peSave`(→`woori_plan_v1`).

라이브 모드가 없으므로 "라이브·헤드리스 일관성"은 더 이상 변수가 아니다(소비자가 하나뿐이다). 일정 시작 주차는 절대값(1~)이다.

**테스트** : `tests/`에 Playwright 스모크 테스트(`smoke.spec.js`)가 있다. node 단위 테스트가 Chart를 스텁해 콜백을 안 태우는 맹점 때문에(빈 화면 사고의 원인), 실제 브라우저에서 두 탭·호버·시나리오를 돌려 콘솔 오류와 미렌더 카드를 잡는다. 루트에 `package.json`을 두지 않아 Vercel 정적 배포는 그대로다. CI는 `.github/workflows/test.yml`.

## 미구현 설계 (문서만, 코드에 없음)

`ONTOLOGY.md`(객체·링크·행동(Action) 레이어·권한 `ROLES`), `SURVEY.md`(측정 설문), `DIAGNOSIS.md`(정의 합의)는 **설계 문서**다. 현재 `index.html`에는 행동 레이어(`careLog`/`runAction`)·역할 권한(`ROLES`/`setRole`)·한 영혼 드릴다운(`selStudent`)·전염 관계 지도가 **없다**. 윤리선(목양이지 사찰 아님)은 지금은 민감 속성을 화면에 아예 띄우지 않음으로 지켜진다(권한 게이트가 아니라 부재로). 이들을 구현하려면 ONTOLOGY의 `ROLES` 게이트가 먼저 와야 한다.

## 모형 규칙 (확장 시 일관성 유지)

기본 보정은 **분당우리교회 중고등부 한 부(약 150명)** 기준이다(교사 ~25명, 연간 신입 ~50명). 1·2부 분리는 아직 안 했다.

**학년은 중1~고3 여섯이다**(`grade` 1~6, `GRADE_LABELS`). 중등(중1~3)·고등(고1~3) 비중은 업로드 명단에서 도출되거나(`rosterToDist`) 시나리오 분포(`DIST`)로 정해지고, `pickGrade6`가 6학년을 뽑는다. 매년(52주) 진급하며 고3(`grade 6`)이 졸업한다. 신입은 중1.

**중3→고1 전환 절벽** : 매년 중3(`grade 3`)이 고1로 올라갈 때, 헌신이 약한 학생 일부가 이탈한다(`cliffProb` = `clamp((0.55-commit)+외톨이0.15+비신앙0.10-코어0.25, 0, 0.7)`). 한국 교회 다음세대 이탈의 가장 큰 지점을 모형에 담은 것. 건강한 부는 거의 안 빠지고, 쇠퇴 중인 부는 전환에서 크게 빠진다.

**수능은 고3(`grade 6`)에만** 깊게 걸린다(시험기간과 별개, 위 '외부 환경' 참조).

**시작은 "현재 진단"이다(갱신 도구의 핵심).** 업로드한 명단의 `출석상태`(활발·느슨·위기)가 `studentFromRow`에서 시작 `commit`/`faith`를 정하고, 명단이 없으면 시나리오 분포 `DIST`의 활발/위기 비율(`active`/`risk`)대로 `spawn`이 뽑는다(코어는 활발 쪽, 외톨이·비신앙은 위기 쪽). 즉 "지금 우리"에서 출발해 "이 계획이 우리를 들어올리는가"를 본다.

### 출석은 2층 구조다

출석을 두 층으로 나눠 실제처럼 천천히 움직이고, 시험·수련회의 일시 변동이 영속 손상으로 굳지 않게 한다.

- `commit`(느린 헌신 0~1) : 사역이 천천히 쌓아 올리는 바탕. 친구 효과도 이 값 기준.
- `eng`(표시 출석) = `clamp(commit + 0.20*retreatGlow - examPress)`. 화면 지표·이탈 판정은 이 값으로 한다.

학생 속성: `grade`, `bg`(mota/normal/none), `core`, `loner`, `commit`, `eng`, `faith`(0~1), `word`·`holy`·`belong`(신앙의 갈래 말씀·거룩·소속 0~1), `owned`·`doubtSpace`(내면화 축 0~1), `adults`(곁의 어른 수), `fam`(가정 신앙 0~1), `sg`, `mentor`, `sermonAcc`(설교 누적), `retreatGlow`(수련회 여운), `examPress`(시험 일시 압박), `friends`, `left`, `dropped`(출석 바닥으로 이탈했는지 — 졸업과 구분해 취약 분석 이탈률에 쓴다).

**내면화 축(owned) — 출석이 가리는 진짜 지표** : `eng`(출석 습관)과 별개로 `owned`(자기 것이 된 믿음, 내재적 신앙 0~1)를 둔다. 출석은 좋아도 `owned`가 낮으면 전환기에 무너진다(NSYR의 "도덕적·치료적 이신론" 위험군). `owned`는 매주 목표값으로 아주 느리게(`sp*(ownT-owned)*0.06`) 이동하며, 목표는 `0.13+0.16*faith+0.24*(word+holy)/2+0.04*min(adults+mentor,3)+0.16*famT2+0.18*processed-0.22*festering-결석페널티`(내재 채널 word·holy를 generic faith보다 무겁게 둔다). 내면화 지수 `ownIndex`(평균×100)·`ownRate`(owned≥0.5 비율). 근거·설문 문항은 `SURVEY.md`.

**모래성(취약도)과 창발하는 전환 절벽** : `fragility(s)=clamp(eng-owned-0.10)`는 지금 출석 중 '신념이 아니라 외부(부모·또래·습관)가 떠받치는 몫'이다(Uecker: 전환기엔 출석만 무너지고 신념은 남는다; Wood 습관은 맥락이 끊기면 무너진다). **`cliffProb`를 손값에서 창발로 바꿨다** — `0.07+0.62*fragility+외톨이·비신앙±코어`. 출석이 높아도 그 출석이 모래성이면(취약도 큼) 크게 빠지고, 내면화된 아이는 출석이 잠시 낮아도 넘어간다(검증: 출석0.8/내면0.2 → 절벽 0.38 vs 출석0.6/내면0.55 → 0.07, 직관 반대). 평균 취약도 `fragIndex`.

**졸업 신앙 잔존(북극성)** : 졸업(고3→나감)은 외부 버팀목이 한꺼번에 사라지는 지점이다. 내면화된 아이만 신앙을 들고 나간다 — `Math.random()<clamp(0.10+0.95*owned,0.03,0.95)`로 `gradKeep`/`gradLapse`를 가른다. `carry=gradKeep/(gradKeep+gradLapse)`가 모든 사역의 최종 성적표다. hist·simulate가 추적, 화면 맨 위 북극성 카드.

**온기×신앙(곱)·의심 U자** : 부모 전수는 가산이 아니라 곱이다(Bengtson; Stearns & McKinney 조절효과). `warmth`(부모 관계 온기 0~1)로 `famT2=(fam-0.5)*(0.4+1.2*warmth)`를 만들어 force·faith·owned에 흘린다 — 차가운 신실함은 잘 전해지지 않는다. `warmth`는 `doubtSpace` 목표도 크게 연다(`dsT=0.18+...+0.30*warmth`). `doubt`(의심)는 사춘기에 자란다(FYI U자): 말할 공간이 있으면 `processed=doubt*doubtSpace`로 내면화 재료가 되고, 막히면 `festering=doubt*(1-doubtSpace)`로 갉는다. 다뤄진 의심은 해소된다.

**믿음 단계·조기경보** : `faithStage(s)`는 owned로 1 부여된(빌려온)·2 탐색·3 내면화(Fowler/Westerhoff 단순화)를 가른다. `earlyWarn(a)`는 아직 출석은 멀쩡한(eng≥0.3) 아이들 속에서 자라는 숨은 위험(취약도·곪는 의심·낮은 owned·외톨이)의 평균으로, 출석 지표보다 먼저 움직이는 선행 신호다. 화면의 "잘 나오지만 속이 빈 아이" 명단(`updateHiddenRisk`)이 이를 사람 단위로 짚는다.

**신앙의 갈래(다면 효과)** : `faith`(종합)와 별개로 `word`(말씀)·`holy`(거룩)·`belong`(소속) 세 갈래가 있다. 매주 각 갈래는 `faith` 쪽으로 천천히 모이되(`0.05*(faith-갈래)`), 설교 결이 갈래마다 다르게 기울인다. **`SERMON_FX`** 표가 결마다 `{eng, faith, word, holy, belong}` 효과를 정의한다 — 여기에 트레이드오프가 산다. `faith`와 생태계 계수는 건드리지 않고(안전), 갈래는 그 위에 얹힌 탐색 렌즈다. 관찰의 "신앙의 결" 줄과 "이번 주 효과"의 `결` 줄에 평균·델타가 뜬다.

매주 `commit` 변화량(헌신에 쌓이는 느린 힘):
```
force = -0.045
  + 0.11*(친구 평균 commit - 0.5)   // 친구 효과
  + (소그룹? 0.06:0)*교사효율
  + (멘토? 0.05:0)*교사효율
  + 0.03*(가정신앙 - 0.5)
  + 약간의 무작위
  + 점이벤트(소그룹집중 +0.06, 봉사 +0.04, 전도 +0.03)
  + 설교 누적(engB*sermonAcc)
commit += speed * force      // speed=변화 속도 슬라이더(0.2~1.0, 기본 0.4)
```
**변화 속도**가 낮을수록 모든 변화가 작고 느려진다(끈기·관성↑). 신앙도 `faith += speed * fd`로 함께 느려진다.

**임계점(티핑, 비선형)** : 친구 효과는 선형(`0.11*(pull-0.5)`)에 더해, 한 아이의 친구 중 흔들리는 비율 `lf`가 절반을 넘으면 `-0.05*(lf-0.5)`로 함께 무너지고(캐스케이드), 든든한 비율 `hf`가 0.6을 넘으면 `+0.03*(hf-0.6)`로 자생 회복한다. 그래서 시작 위기를 똑같이 키워도 이탈은 **가속**한다(현재 52주 엔진·150명 기준 방향값: 시작 위기 12→22→32%에서 누적 이탈 16→18→25명, 증분 +2 → +7로 가속). 흔들리는 군집을 깨거나 거점을 심는 작은 개입이 큰 차이를 내는, 안 뻔한 결과의 원천.

**수확 체감·포화(비선형)** : 한 아이의 버팀목 수 `sup`(소그룹·멘토·든든한 친구·믿는 가정)이 많을수록 `sat=1-0.15*(sup-1)`로 소그룹·멘토 효과가 줄어든다(첫 버팀목은 온전히, 그 위로는 덜). 그래서 이미 든든한 아이에게 몰빵하기보다 **버팀목 없는 아이에게 퍼뜨릴 때** 합이 크다 — 한 레버 몰빵 < 배합(현재 52주 엔진 기준 방향값: 소그룹 약할 때 멘토↑ 활발 +15 vs 든든할 때 +13으로, 버팀목 없는 쪽 이득이 더 크다. 차이는 옛 24칸 엔진보다 작다).

**입력은 두 종류다 — 우리가 정하는 사역(지렛대)과 닥치는 외부 환경.** 설교·수련회·전도·소그룹·봉사는 교역자가 정한다. **시험기간·수능**은 닥치는 외부 환경이다(연간 계획 폼에서 optgroup으로 갈라 둠). 이 입력들이 관찰·시나리오의 출석·신앙(출력)을 움직인다.

**시험기간(일시 변동, 영속 아님)** : 일정에 `weeks`(기본 3주)와 `repeat`를 둔 기간 이벤트. 기간 동안 `examPress`가 매주 `+0.14`(상한 0.30) 쌓여 표시 출석을 끌어내리고, 끝나면 `×0.45`로 빠르게 풀려 회복된다. 기간이 이탈 기준(6주)보다 짧아 시험 단독으로는 이탈하지 않는다.

**수능(고3, 외부 환경)** : 일정에 `weeks`(기본 6주)·`repeat`(52=매년). 그 시즌 동안 **고3은 깊게**(`examPress +0.20`, 상한 0.45) 빠지고 다른 학년은 옅게(`+0.05`, 상한 0.18). 끝나면 회복되지만 고3은 곧 졸업해 떠난다. 시험기간과 같은 `examPress` 채널을 쓰되 학년별로 다르게 누른다.

**수련회(상세)** : 일정에 `nights`(1·2·3박), `attendRate`, `intensity`. `factor = (0.45 + 0.22*nights) * (0.5 + 0.6*intensity)`. 그 주에 학생은 `attendRate`(코어·소그룹원 ×1.3) 확률로 참가하고, 참가자만 `retreatGlow = max(현재, factor)`(일시 급등)와 `commit += 0.02*factor`(작은 영속 상승)를 받는다. `retreatGlow`는 매주 `×0.85`로 식는다. 비참가자는 못 받아 격차가 생긴다.

**설교 시리즈** : 일정에 `focus`와 `weeks`. 초점은 여섯 — 양육·전도·회복·**말씀·거룩·은혜**(`SERMON_FOCUS`). 즉효가 아니라 시차를 두고 `sermonAcc`(매주 `+=(1-acc)*0.15`, 안 닿으면 `×0.92`)에 비례해 누적된다. 효과는 결마다 다르다(`SERMON_FX`, 트레이드오프): **거룩·순결형**은 거룩을 크게 올리되 출석(force)·소속을 누르고, **말씀형**은 말씀을 깊게 올리되 출석을 살짝 누르며, **은혜·위로형**은 소속·출석을 올리되 거룩은 더디다. 양육·전도·회복형은 신앙(`+0.022*acc`)과 출석을 함께 올린다(기존과 동일). 시리즈가 끝나도 여운이 남는다.

**상호작용(설교×관계 뿌리)** : 설교 효과는 한 아이가 관계에 얼마나 뿌리내렸나(`root=0.85+소그룹 0.18+친구수×0.04`)에 곱해진다. 소그룹·친구가 든든한 아이에겐 더 박히고, 외톨이에겐 덜 박힌다. 그래서 외톨이는 "더 좋은 설교"가 아니라 **관계로 먼저 잇는 것**이 답 — 같은 레버라도 결과가 아이마다·부서마다 달라지는(안 뻔한) 비선형이다. `root` 평균은 ≈1로 두어 전체 보정은 유지.

### 3층 교회 생태계 (중고등부는 진공이 아니다)

고등학생의 신앙·출석은 청소년 사역만으로 정해지지 않는다. 위 두 층이 천장을 정한다.

- **① 전체 교회** : 별도 트랙 `churchSchedule`(담임 말씀 시리즈·전교인 수련회·부흥회·절기)이 `churchClimate`(0~1)를 천천히 끌어올린다. 목표값 = `0.45 + 이벤트 부스트`, 매주 그쪽으로 `sp*0.25` 이동.
- **② 가정·부모** : `parentVitality`(부모 다락방 활성도) 목표 = `0.22 + 0.46*churchClimate + 0.30*다락방참여율`, 매주 `sp*0.20` 이동.
- **③ 중고등부** : 각 학생 `fam`이 `parentVitality`(+배경 보정: 모태 +0.12, 비신앙 −0.18) 쪽으로 매주 `sp*0.10` 이동한다(이제 고정값이 아니다). `fam`은 출석힘에 `0.05*(fam−0.5)`, 신앙에 `0.020*(fam−0.5)`. 더해 `churchClimate`가 직접 출석 `0.035*(cc−0.5)`·신앙 `0.015*(cc−0.5)`를 받친다.
- **전교인 수련회**(`allRetreat`) : 온 가족 참여 → 모든 학생에게 수련회 여운(glow 0.9)+commit·신앙 상승. **부흥회**(`revival`) : 그 기간 출석 `+0.05`·신앙 `+0.02`.

이로써 전체 교회·부모가 살아나면 중고등부가 눈에 띄게 따라 오르고, 청소년 사역만으로는 닿지 못하는 천장이 보인다. 수치는 전부 가정값 — 정밀 예측이 아니라 "무엇이 천장을 정하는가"의 방향으로 읽는다.

**교사 소진·과부하** : `teacherEff(tc,n)=clamp(1-max(0,n/tc-6)*0.06,0.2,1)`. 1교사당 6명까지는 온전(1.0), 그 위로 한 명당 6%씩 떨어지고 바닥 0.2. 그래서 교사를 무한정 늘려도 1.0에서 멈추고(몰빵 한계), 너무 적으면 급격히 무너진다(현재 52주 엔진·150명 기준 방향값: 교사 10명 이탈 25 vs 18명 11 vs 30명 12로, 모자라면 급증하고 18명 위로는 거의 안 준다). 라이브·헤드리스 공유.

표시 출석이 12주 연속 바닥(0.12 미만)이면 이탈 처리된다(`LEAVE_AFTER=12`, 약 3개월). **1년 = 52주(실제 달력)**이며, 한 칸 = 실제 1주다. 계수는 옛 24칸/년 기준으로 검증됐으므로 칸당 변화율을 `STEP=24/52`로 줄여 연 단위 동역학을 보존한다(`tick`의 `sp=ctx.speed*STEP`, `advanceChurch`도 동일). 일정의 시작·반복 주차는 실제 달력 주(1주차 ≈ 3월): 시험 연 4회(repeat 13), 수련회·수능·담임 시리즈 연 1회(repeat 52). 전망·시나리오는 1년=52주(`buildScenario`가 6회 평균)다. 52주마다 학년이 오르고 고3은 졸업해 빠지며 신입생이 들어온다.

## 중요한 한계 (반드시 유지할 전제)

위의 계수(0.18, 0.11 등)는 모두 **임의로 설정한 가정값**이며 실제 데이터로 검증되지 않았다. 이 수치를 사실처럼 다루지 말 것. 실제 결론을 내리는 용도가 아니라 가설을 다듬는 용도임을 코드 주석이나 화면 문구에 유지하라. 어떤 구성요소가 문헌으로 얼마나 받쳐지는지(구조·부호는 받쳐지나 크기는 가정)는 `METHODOLOGY.md` §8 "증거 등급화"에 등급·출처와 함께 정리돼 있다.

**알려진 정합성 이슈(다음 작업).** 적대적 리뷰에서 나온, 고칠 모형·표시 정합성:
- ~~`examPress`가 `STEP` 미적용이라 시험 주 급락이 과대했다.~~ **(완료)** 누적·회복을 `STEP`에 태워 단일 주 급락을 완화(시험 주 최악 낙폭 약 −38→−15). `retreatGlow`·`sermonAcc`는 아직 미적용(commit/faith·설교 비교에 얽혀 보류).
- ~~활발/위기 임계값이 세 곳에서 달랐다(0.55/0.34/0.30).~~ **(완료)** 명명 상수 `ACTIVE_MIN`(0.55)·`RISK_MAX`(0.30)로 한 곳에 합쳤다(`rosterToDist`의 0.34도 0.30으로).
- 시험 주 잔여 낙폭은 임계값(0.55) 근처에 몰린 인구가 일제히 넘나드는 **이산화 군집 인공물**이다(부드러운 멤버십이나 띠 중심 이동이 다음 단계).
- 시드 명단은 시작값(`commit`/`faith`/`owned` 등)을 파싱 때 한 번만 뽑아, 6회 띠가 시작 불확실성을 과소평가한다(`cloneStudent`에서 결마다 다시 뽑아야 한다).
- `carry`(북극성)는 1회 졸업 사건 기반이라 회차 변동이 크다(점이 아니라 띠로 보여야 한다).
- "검증"이라는 말은 외부 데이터 검증이 아니라 모형 내부 일관성 확인을 뜻한다.

## 작성 규칙

- UI 텍스트와 주석은 한국어, 신문 칼럼체(~다/~이다). 대화체 종결어미(~이에요/~거든요) 사용 금지.
- 글에 em dash(—) 사용 금지.
- 화면에 나오는 모든 숫자는 반올림 처리(`Math.round` 등).
- 비주얼은 깔끔한 현대 대시보드(밝은 배경·흰 카드·차분한 색·읽기 쉬운 sans-serif). 색은 현재 `PALETTE` 객체와 인라인 hex로 흩어져 있다(`:root` CSS 변수는 아직 없다, 정리 대상).
- 전문 용어(SIR·Rₜ·포트폴리오·VaR 등)는 쉬운 말로 옮겨 쓴다(목사가 주 사용자). 모든 수치는 "그냥 넣어 본 값, 방향으로만"의 한계를 유지.

## 좋은 다음 작업 (사용자가 요청할 만한 것)

1. **1·2부 분리** : 각 ~150명을 부로 나눠 따로 돌리고 부별/합계를 본다(친구·교사·정원이 부 안에서 닫힘). 사용자가 다음 단계로 남겨 둔 것.
2. ~~실제 중고등부 출석 데이터(CSV)를 불러와 시작 분포를 보정하기.~~ **(완료)** 명단 CSV(`이름,학년,신앙배경,코어,외톨이,출석상태`)를 업로드하면 그 학생들이 시뮬레이션 시작 인구가 되고(랜덤 `spawn` 대신 `cloneStudent`로 시드), 모든 카드·차트·취약 분석·토네이도가 이 백데이터에서 출발한 결과를 읽는다. `parseCSV`→`studentFromRow`(행→시작 속성)→`makeDataDef`(`rosterToDist`로 분포 도출)→`SC_DEFS.data`. `simulateOnce`는 `cfg.seed`가 있으면 시드 인구를, 없으면 옛 `spawn` 경로를 쓴다(두 경로 공존). 헤더의 `📁 명단 CSV 업로드`(`onCSV`/`loadRoster`)와 `우리 부 데이터` 시나리오가 기본. 업로드 전엔 내장 `SAMPLE_CSV`(=`sample-data.csv`)를 보여 준다. 신규 유입·전도 신입은 도출된 분포대로 계속 `spawn`한다. 계수 보정은 아직(다음 단계).
3. 예측 결과·처방을 목회 보고용 한 장짜리 리포트(또는 PDF)로 내보내는 버튼.
4. 연간 계획·운영·교회 설정을 저장/불러오기(localStorage 또는 JSON).
5. ~~예측 결과에 신뢰구간 표시.~~ **(완료)** `simulate`가 6회 최소~최대 띠를 함께 반환하고 차트에 `bandDS`로 평균선 뒤에 깐다.
6. 시험기간 출석 하락폭(`examPress` 상한 0.30)과 설교 초점별 효과를 실제 경험에 맞게 미세 조정.
